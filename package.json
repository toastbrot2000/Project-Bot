{
    "name": "project-bot-monorepo",
    "private": true,
    "scripts": {
        "dev": "pnpm --filter \"./apps/*\" --parallel dev",
        "build": "pnpm --filter \"./apps/*\" build",
        "lint": "pnpm --filter \"./apps/*\" lint",
        "optimize-assets": "node scripts/optimize-images.js"
    },
    "devDependencies": {
        "sharp": "^0.34.5",
        "typescript": "^5.0.0",
        "vite": "^5.0.0"
    }
}